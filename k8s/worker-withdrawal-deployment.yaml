apiVersion: apps/v1
kind: Deployment
metadata:
  name: tadpole-worker-withdrawal
  labels:
    app: tadpole-worker-withdrawal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tadpole-worker-withdrawal
  template:
    metadata:
      labels:
        app: tadpole-worker-withdrawal
    spec:
      containers:
        - name: worker
          image: tadpole-api:latest
          imagePullPolicy: IfNotPresent
          command: ["node", "dist/workers/withdrawalWorker.js"]
          envFrom:
            - configMapRef:
                name: tadpole-config
            - secretRef:
                name: tadpole-secrets
                optional: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
